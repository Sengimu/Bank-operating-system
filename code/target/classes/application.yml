server:
  port: 22987 #指定端口号
spring:
  application:
    name: login-service
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 5000MB
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/bank?useSSL=false&serverTimezone = GMT
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver